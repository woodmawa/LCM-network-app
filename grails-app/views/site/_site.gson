import com.softwood.domain.Site

//default template - just renders id and name
model {
    Site site
}

json jsonapi.render (site, [jsonApiObject:true, expand: "locations"])


/*

exploration only
json.'_siteTmpl' {

    id site.id
    org site.org.name
    name site.name
}
*/
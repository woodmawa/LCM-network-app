import com.softwood.domain.Site

//default template - just renders id and name
model {
    Site site
}

json jsonapi.render (site, [expand: "locations"])


/*

exploration only
json.'_siteTmpl' {

    id site.id
    org site.org.name
    name site.name
}
*/
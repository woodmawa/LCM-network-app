import com.softwood.domain.Device

//this gets invoked by default show method on controller
//inherits template:"parent"
model {
    Device device
}
//first map entru is named variable passed to template
//json g.render (template:"device", model:[device:device])
//json jsonapi.render (device, [jsonApiObject:true])
//json ([1,2,3])
json tmpl.device (device)
